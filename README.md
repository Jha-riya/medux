medux is a headless media SDK built on the Pexels API, split into cleanly separated layers: a framework-agnostic core (media-core), thin platform wrappers for React and React Native, and independent headless UI component libraries with zero styling baked in. A demo web app wires the React wrapper and component library together to power search, an infinite-scroll grid, a lightbox, and a vertical reel viewer.
